package de.jupetersen.shop;

public class WasMachtJedeZeile {

	// 30. Wird eine Klasse Namens Shop erstellt.

	// 32. Erstellen einer Klassen Variabel vom Daten Typ JFrame
	// mit Namen Frame.

	// 33. Erstellen einer Klassen Variable vom Daten Typ warenkorb
	// mit dem Namen Warenkorb.

	// 36. Erstellung der main-Methode. Sie ist die Hauptmethode jedes
	// Java Programms, und wird beim Start des Programms aufgerufen.

	// 41. System.out.println(" Willkommen in meinem Shop.")
	// wird der Text auf der Konsole augegeben.

	// 43. Es wird eine Methoden Variable Namens produkte und dem
	// Daten Typ List. In den < > von List wird der Daten Typ
	// angegeben. Desen Objekte können in der Liste abgelegt werden.
	// Anschlieschend wird die Methode produkteAnlegen aufgerufen
	// und desen Rückgabewert in der Variable produkte gespeichert.

	// 45. Die Methode kundeAnlegen wird aufgerufen und desen
	// Rückgabewert wird in der erstellten Methoden Variable
	// Kunde gespeichert. Die Variable ist vom Daten Typ Kunde.

	// 47. Die Methode warenkorbFüllen wird aufgerufen, und desen
	// Rückgabewert in der Klassen Variable gespeichert.
	// Und als Parameter wird kunde und produkte mitgegeben.

	// 49. Hier wird an dem frame die add Methode aufgerufen und
	// ein BorderLayout.NORTH hinzugefügt.

	// 58-66 Hier wird die Methode warenkorbFüllen mit dem Daten Typ
	// warenkorb erstellt, und als Parameter den Daten Typ Kunde
	// und List Produkte mitgegeben. Dann wurde ein neuer
	// Warenkorb erstellz
	
	// 69-81 Hier wird eine neue Methode erstelltnit dem Namen panelNorth
	// vom Daten Typ JPanel und dann wir noch ein neues MigLayout erzeugt.
	// Danach wurde ein JLabel erzeugt mit dem Namen label und dem 
	// Daten Typ JLabel. Dann wurde die panelNorth.add Megthode aufgereufen
	// und den label nach Strings mitgegeben, und zum Schluss wurden noch 
	// 1 JButton erstellt mit dem Namen shop und dem Daten Typ JButton,
	// 2 JButton erstellt mit dem Namen warenkorb und dem 
	// Daten Typ JButton. Dem JButton warenkorb wurde noch eine 
	// AbstractAction hinzugefügt.
	
	// 83-95 Hier wurden 4 neue JButton erstellt und dem JButton service 
	// noch eine AbstractAction hinzugefügt.

}
